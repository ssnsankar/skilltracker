const baseUrl = 'http://localhost:52100/api'
const apiName = 'skill-tracker'
const api = 'api'
const version = 'v1'
const routers = {
    admin: 'admin',
    skillDetail: 'skillDetail'
}

export {
    baseUrl,
    apiName,
    api,
    version,
    routers
}